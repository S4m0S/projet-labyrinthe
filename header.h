#include <string.h>
#include <stdio.h>
#include <stdlib.h>

typedef struct position{
    int x;
    int y;
};

typedef struct tresor{
    position* piece;
    bool find;
    char affiche;
};

typedef struct tresor{
    position* piece;
    bool find;
    char affiche;
};

typedef struct joueur {
    position* piece;
    bool trouve;
    char affichage;
    
};